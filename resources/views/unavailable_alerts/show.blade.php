@extends('layouts.backend')

@section('content')
    <div class="container">
        <div class="row">
            @include('admin.sidebar')

            <div class="col-md-9">
                <div class="card">
                    <div class="card-header">Unavailable_alert {{ $unavailable_alert->id }}</div>
                    <div class="card-body">

                        <a href="{{ url('/unavailable_alerts') }}" title="Back"><button class="btn btn-warning btn-sm"><i class="fa fa-arrow-left" aria-hidden="true"></i> Back</button></a>
                        <a href="{{ url('/unavailable_alerts/' . $unavailable_alert->id . '/edit') }}" title="Edit Unavailable_alert"><button class="btn btn-primary btn-sm"><i class="fa fa-pencil-square-o" aria-hidden="true"></i> Edit</button></a>
                        {!! Form::open([
                            'method'=>'DELETE',
                            'url' => ['unavailable_alerts', $unavailable_alert->id],
                            'style' => 'display:inline'
                        ]) !!}
                            {!! Form::button('<i class="fa fa-trash-o" aria-hidden="true"></i> Delete', array(
                                    'type' => 'submit',
                                    'class' => 'btn btn-danger btn-sm',
                                    'title' => 'Delete Unavailable_alert',
                                    'onclick'=>'return confirm("Confirm delete?")'
                            ))!!}
                        {!! Form::close() !!}
                        <br/>
                        <br/>

                        <div class="table-responsive">
                            <table class="table">
                                <tbody>
                                    <tr>
                                        <th>ID</th><td>{{ $unavailable_alert->id }}</td>
                                    </tr>
                                    <tr><th> State </th><td> {{ $unavailable_alert->state }} </td></tr><tr><th> Order Id </th><td> {{ $unavailable_alert->order_id }} </td></tr><tr><th> User Id </th><td> {{ $unavailable_alert->user_id }} </td></tr>
                                </tbody>
                            </table>
                        </div>

                    </div>
                </div>
            </div>
        </div>
    </div>
@endsection
